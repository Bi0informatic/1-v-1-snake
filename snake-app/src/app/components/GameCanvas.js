import React from 'react'

const boardBackground = "#b7a148"
const snakeColor = "#485eb7"
const snakeBorder = "black";
const foodColor = "red";
const unitSize = 25;
const canvasSize = 500;

export default function GameCanvas({snake, food, running}) {
    return <canvas id="game-canvas" width="500" height="500">Your browser does not support the HTML5 canvas tag.</canvas>;
}